const PEFSO_LOGO='https://newpefso.github.io/info/assets/brand/pefso-icon-square.png';const PEFSO_CONTACT={name:'PEFSO CO., LTD.',address:'71/43 Chua Boc, Kim Lien Ward, Hanoi, Vietnam',email:'thangmq@pefso.com',web:'www.pefso.com'};let currentPrintName='PEFSO_Document';function slugDoc(s){return String(s||'Buyer').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^A-Za-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,42)||'Buyer'}function dateStamp(){const d=new Date();return d.getFullYear()+String(d.getMonth()+1).padStart(2,'0')+String(d.getDate()).padStart(2,'0')}function uniqueDocName(type,company,ref){return `PEFSO_${type}_${slugDoc(company)}_${dateStamp()}_${slugDoc(ref||String(Date.now()).slice(-6))}`}function docFooter(){return `<div class="doc-footer"><div><b>${PEFSO_CONTACT.name}</b><br>${PEFSO_CONTACT.address}</div><div>${PEFSO_CONTACT.email}<br>${PEFSO_CONTACT.web}</div></div>`}function docHeader(type,ref,status){return `<div class="doc-topbar"></div><div class="doc-header"><div class="doc-brand"><img class="doc-logo" src="${PEFSO_LOGO}"><div><div class="doc-company">PEFSO · VIETNAM</div><div style="font-size:10px;color:#74807b;margin-top:3px">Manufacturing · Sourcing · OEM</div></div></div><div class="doc-meta"><b>${type}</b><br>Reference: ${esc(ref)}<br>Date: ${new Date().toLocaleDateString('en-GB')}${status?`<br><span class="doc-status">${esc(status)}</span>`:''}</div></div>`}function selectedList(){return [...selectedProducts].map(id=>products.find(p=>p.product_id===id)).filter(Boolean)}function renderSelectionTray(){let tray=document.getElementById('selectionTray');if(!tray){tray=document.createElement('div');tray.id='selectionTray';tray.className='selection-tray hidden';document.getElementById('products').appendChild(tray)}const ps=selectedList();tray.classList.toggle('hidden',!ps.length);tray.innerHTML=`<div class="selection-main"><div class="selection-count">${ps.length}</div><div><div style="font-weight:750;font-size:12px">Selected products</div><div class="selection-chips">${ps.map(p=>`<span class="sel-chip">${esc(p.item_code)} · ${esc(p.color)}</span>`).join('')}</div></div></div><div class="selection-actions"><button class="btn light sm" onclick="clearSelection()">Clear</button><button class="btn light" onclick="goToProposal()">Build proposal</button><button class="btn o" ${ps.length!==1?'disabled title="Select exactly one product for a quotation"':''} onclick="goToQuotation()">Create quotation</button></div>`}function clearSelection(){selectedProducts.clear();renderProducts();renderSelectionTray()}function goToProposal(){go('proposal');renderProposal()}function goToQuotation(){const ps=selectedList();if(ps.length!==1)return alert('Select exactly one product for a quotation.');go('quotation');$('qProduct').value=ps[0].product_id;renderQuote()}const _toggleProduct=toggleProduct;toggleProduct=function(id){_toggleProduct(id);renderSelectionTray()};renderProducts=function(){const show=products.length?products:DEMO_PRODUCTS;$("productGrid").innerHTML=show.map(p=>{const on=selectedProducts.has(p.product_id);return `<div class="card product ${on?'is-selected':''}" onclick="if(!event.target.closest('button'))toggleProduct('${esc(p.product_id)}')"><div class="select-check">✓</div><div class="photo"><img loading="lazy" src="${esc(p.image_url||'')}" alt="${esc(p.color||'')}"></div><div class="pb"><div class="code">${esc(p.item_code)}</div><div class="product-title">${esc(p.color)}</div><div class="sub">${esc(p.product_name||'Small Foil Balloon Weight')}</div><div class="sub" style="margin-top:3px">MOQ ${Number(p.moq_pcs||97000).toLocaleString()} pcs</div>${admin()?`<div class="internal">Expected $${Number(p.expected_fob_usd||0).toFixed(6)} · Floor $${Number(p.floor_fob_usd||0).toFixed(6)}</div>`:''}<div class="rowactions" style="margin-top:8px"><button class="btn ${on?'o':'light'} sm" onclick="event.stopPropagation();toggleProduct('${esc(p.product_id)}')">${on?'✓ Added':'Add'}</button>${can('can_manage_products')?`<button class="btn light sm" onclick='event.stopPropagation();openProduct(${JSON.stringify(JSON.stringify(p))})'>Edit</button>`:''}</div></div></div>`}).join('');$("pProducts").innerHTML=selectedProducts.size?selectedList().map(p=>`<span class="badge" style="margin:0 4px 4px 0">${esc(p.item_code)} · ${esc(p.color)}</span>`).join(''):'Select products from the Products tab.';renderSelectionTray()};function proposalRef(c){return `PROP-${dateStamp()}-${String(c?.customer_id||'DRAFT').replace(/[^A-Za-z0-9]/g,'').slice(-6)}`}renderProposal=function(){const c=customers.find(x=>x.customer_id===$("pCustomer").value);const ps=selectedList();const ref=proposalRef(c);currentPrintName=uniqueDocName('Proposal',c?.company_name,ref);$("proposalPreview").innerHTML=`<div class="docpage">${docHeader('COMMERCIAL PROPOSAL',ref,'FOR DISCUSSION')}<div class="doc-kicker">Balloon Weight · Manufacturing & OEM</div><h1 class="doc-title">A focused Vietnam supply proposal for ${esc(c?.company_name||'your team')}</h1><div class="doc-lead">PEFSO proposes a practical starting collection built around the current Small Foil Balloon Weight platform, with room for private-label, color and packaging adaptation as the program develops.</div><div class="doc-grid2"><div class="doc-box"><div class="lab">Prepared for</div><div class="val">${esc(c?.company_name||'Buyer')}</div><div class="doc-copy">${esc(c?.contact_name||'')} ${c?.country?`· ${esc(c.country)}`:''}</div></div><div class="doc-box"><div class="lab">PEFSO contact</div><div class="val">Thang Mai</div><div class="doc-copy">${PEFSO_CONTACT.email}</div></div></div><div class="doc-section"><h3>Buyer context</h3><div class="doc-copy">${esc($("pContext").value||'Target buyer requirements to be validated during commercial discussion.')}</div></div><div class="doc-section"><h3>Recommended direction</h3><div class="doc-copy">${esc($("pDirection").value)}</div></div><div class="doc-section"><h3>Recommended collection</h3><div class="doc-products">${ps.length?ps.map(p=>`<div class="doc-product"><img src="${esc(p.image_url||'')}"><div><div class="sku">ITEM ${esc(p.item_code)}</div><div class="name">${esc(p.color)} · ${esc(p.product_name)}</div><div class="spec">MOQ ${Number(p.moq_pcs||97000).toLocaleString()} pcs<br>Standard cement core · metallic foil finish</div></div></div>`).join(''):'<div class="doc-callout">Select products in the Product Catalogue before issuing this proposal.</div>'}</div></div><div class="doc-section"><h3>PEFSO execution scope</h3><div class="doc-grid2"><div class="doc-box"><div class="val">OEM / Private label</div><div class="doc-copy">Color program, bottom label / barcode, artwork and retail packaging adaptation.</div></div><div class="doc-box"><div class="val">QC & shipment readiness</div><div class="doc-copy">Incoming material, core consistency, assembly, appearance, labeling and packing control.</div></div></div></div><div class="doc-section"><h3>Next step</h3><div class="doc-callout">${esc($("pNext").value)}</div></div>${docFooter()}</div>`};renderQuote=function(){const c=customers.find(x=>x.customer_id===$("qCustomer").value),p=products.find(x=>x.product_id===$("qProduct").value),qty=Number($("qQty").value||0),pr=Number($("qPrice").value||0);const ref=`QT-${dateStamp()}-${String(c?.customer_id||'DRAFT').replace(/[^A-Za-z0-9]/g,'').slice(-6)}`;currentPrintName=uniqueDocName('Quotation',c?.company_name,ref);const total=qty*pr;$("quotePreview").innerHTML=`<div class="docpage">${docHeader('QUOTATION',ref,'PENDING ADMIN APPROVAL')}<div class="doc-kicker">Commercial offer</div><h1 class="doc-title">Quotation</h1><div class="doc-grid2"><div class="doc-box"><div class="lab">Buyer</div><div class="val">${esc(c?.company_name||'')}</div><div class="doc-copy">${esc(c?.contact_name||'')}<br>${esc(c?.country||'')}</div></div><div class="doc-box"><div class="lab">Supplier</div><div class="val">${PEFSO_CONTACT.name}</div><div class="doc-copy">${PEFSO_CONTACT.address}</div></div></div><div class="doc-section"><h3>Commercial line item</h3><table class="doc-table"><thead><tr><th>Item</th><th>Description</th><th>Qty</th><th>Unit price</th><th>Amount</th></tr></thead><tbody><tr><td><b>${esc(p?.item_code||'—')}</b></td><td>${esc(p?.color||'')}<br><span style="color:#6c7772">${esc(p?.product_name||'Small Foil Balloon Weight')}</span></td><td>${qty?qty.toLocaleString():'—'} pcs</td><td>${pr?'$'+pr.toFixed(6):'Pending'}</td><td>${pr?'$'+total.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2}):'Pending'}</td></tr></tbody></table>${pr?`<div class="doc-total"><div class="doc-total-box"><span>Total quotation value</span><b>$${total.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div></div>`:''}</div><div class="doc-section"><h3>Commercial terms</h3><div class="doc-grid2"><div class="doc-box"><div class="lab">Incoterm</div><div class="val">${esc($("qTerm").value)}</div></div><div class="doc-box"><div class="lab">Destination port</div><div class="val">${esc($("qPort").value||'To be confirmed')}</div></div><div class="doc-box"><div class="lab">Payment</div><div class="val">${esc($("qPayment").value)}</div></div><div class="doc-box"><div class="lab">Lead time</div><div class="val">${esc($("qLead").value)}</div></div><div class="doc-box"><div class="lab">Validity</div><div class="val">${esc($("qValidity").value)} days</div></div><div class="doc-box"><div class="lab">MOQ reference</div><div class="val">${Number(p?.moq_pcs||97000).toLocaleString()} pcs</div></div></div></div><div class="doc-section"><div class="doc-callout">This quotation becomes official only after PEFSO Admin approval. Final color, specification, mixed-SKU loading and packing are subject to approved sample and production confirmation.</div></div><div class="doc-sign"><div><div class="line">Prepared by · PEFSO Sales</div></div><div><div class="line">Approved by · PEFSO Admin</div></div></div>${docFooter()}</div>`};printSection=function(id){if(id==='proposal')renderProposal();if(id==='quotation')renderQuote();const old=document.title;document.title=currentPrintName;document.querySelectorAll('.section').forEach(s=>s.classList.toggle('printme',s.id===id));setTimeout(()=>{window.print();setTimeout(()=>{document.title=old;document.querySelectorAll('.section').forEach(s=>s.classList.remove('printme'))},400)},80)};saveProposal=async function(){renderProposal();try{const r=await api("saveProposal",{customer_id:$("pCustomer").value,deal_id:$("pDeal").value,buyer_context:$("pContext").value,recommended_direction:$("pDirection").value,next_step:$("pNext").value,selected_product_ids:[...selectedProducts]});if(!r.ok)throw Error(r.error);currentPrintName=uniqueDocName('Proposal',customers.find(x=>x.customer_id===$("pCustomer").value)?.company_name,r.proposal_id);message("proposalMsg","Saved · "+r.proposal_id,true);loadDashboard()}catch(e){message("proposalMsg",e.message)}};saveQuote=async function(){renderQuote();try{const r=await api("saveQuotation",{customer_id:$("qCustomer").value,deal_id:$("qDeal").value,product_id:$("qProduct").value,quantity:$("qQty").value,requested_unit_price:$("qPrice").value,destination_port:$("qPort").value,incoterm:$("qTerm").value,validity_days:$("qValidity").value,payment_terms:$("qPayment").value,lead_time:$("qLead").value});if(!r.ok)throw Error(r.error);currentPrintName=uniqueDocName('Quotation',customers.find(x=>x.customer_id===$("qCustomer").value)?.company_name,`${r.quotation_no}_V${r.version}`);message("quoteMsg",`Saved · ${r.quotation_no} V${r.version}`,true);loadQuotes();loadDashboard()}catch(e){message("quoteMsg",e.message)}};function overdueCount(ds){const t=new Date().toISOString().slice(0,10);return ds.filter(d=>!['Won','Lost'].includes(d.stage)&&d.next_action_date&&String(d.next_action_date).slice(0,10)<t).length}async function loadDashboard(){const cs=scopedDemo(customers.length?customers:DEMO_CUSTOMERS),ds=scopedDemo(deals.length?deals:DEMO_DEALS);if(admin()){$('dashboard').innerHTML=`<div class="toolbar"><div><div class="ey">Executive control</div><h2 style="margin:3px 0">Sales command center</h2><div class="sub">Exceptions, approvals and team execution — nothing more.</div></div><button class="btn light sm" onclick="loadDashboard()">Refresh</button></div><div id="adminMetrics" class="admin-control-grid" style="margin-top:10px"></div><div class="control-split"><div class="card"><div class="ey">Team effort</div><h3 style="margin:4px 0 8px">Execution by owner</h3><div id="teamWrap"></div></div><div class="card"><div class="ey">Management attention</div><h3 style="margin:4px 0 8px">Needs action</h3><div id="attentionWrap" class="compact-list"></div></div></div><div class="card" style="margin-top:10px"><div class="ey">Pipeline</div><div id="pipelineWrap" style="margin-top:8px"></div></div>`;const pending=DEMO_QUOTES.filter(q=>q.status==='Pending Approval').length,warn=cs.filter(c=>String(c.protection_state).toLowerCase()==='warning').length,exp=cs.filter(c=>String(c.protection_state).toLowerCase()==='expired').length,req=ds.filter(d=>String(d.request_won).toLowerCase()==='true'&&d.stage!=='Won').length,overdue=overdueCount(ds);$('adminMetrics').innerHTML=`<div class="metric"><div class="label">Open deals</div><div class="value">${ds.filter(d=>!['Won','Lost'].includes(d.stage)).length}</div></div><div class="metric attn"><div class="label">Quote approvals</div><div class="value">${pending}</div></div><div class="metric attn"><div class="label">Won requests</div><div class="value">${req}</div></div><div class="metric ${overdue?'bad':''}"><div class="label">Overdue follow-ups</div><div class="value">${overdue}</div></div><div class="metric ${exp?'bad':''}"><div class="label">Lead risk</div><div class="value">${warn+exp}</div></div>`;const users=['ctv1','ctv2','staff'];$('teamWrap').innerHTML='<table><tr><th>Owner</th><th>Activities</th><th>Customers</th><th>Open</th><th>Won</th></tr>'+users.map(u=>`<tr><td><b>${u}</b></td><td>${DEMO_ACTIVITIES.filter(a=>a.user_id===u).length}</td><td>${cs.filter(c=>c.owner_user_id===u).length}</td><td>${ds.filter(d=>d.owner_user_id===u&&!['Won','Lost'].includes(d.stage)).length}</td><td>${ds.filter(d=>d.owner_user_id===u&&d.stage==='Won').length}</td></tr>`).join('')+'</table>';$('attentionWrap').innerHTML=`<div class="compact-row"><div><div class="title">Quotation approval</div><div class="meta">Official price control</div></div><span class="badge">${pending}</span></div><div class="compact-row"><div><div class="title">Lead warning / expired</div><div class="meta">30 / 90-day protection</div></div><span class="badge">${warn} / ${exp}</span></div><div class="compact-row"><div><div class="title">Overdue next actions</div><div class="meta">Follow-up discipline</div></div><span class="badge">${overdue}</span></div>`;const pipe={};ds.forEach(d=>pipe[d.stage]=(pipe[d.stage]||0)+1);$('pipelineWrap').innerHTML='<div class="grid4">'+Object.entries(pipe).map(([k,v])=>`<div class="metric"><div class="label">${esc(k)}</div><div class="value">${v}</div></div>`).join('')+'</div>';try{const r=await apiFast('dashboard',{},1700);if(r.ok&&r.team){$('teamWrap').innerHTML='<table><tr><th>Owner</th><th>Meaningful activity</th><th>Customers</th><th>Open</th><th>Won</th></tr>'+r.team.map(x=>`<tr><td><b>${esc(x.user_id)}</b></td><td>${x.meaningful_7d||0}</td><td>${x.customers||0}</td><td>${x.open_deals||0}</td><td>${x.won||0}</td></tr>`).join('')+'</table>'}}catch{}return}const mine=me&&me.role==='collaborator'?ds.filter(d=>d.owner_user_id===me.user_id):ds;$('dashboard').innerHTML=`<div class="grid4"><div class="card kpi"><span>Open deals</span><b>${mine.filter(d=>!['Won','Lost'].includes(d.stage)).length}</b></div><div class="card kpi"><span>Follow-ups due</span><b>${overdueCount(mine)}</b></div><div class="card kpi"><span>Selected products</span><b>${selectedProducts.size}</b></div><div class="card kpi"><span>Won</span><b>${mine.filter(d=>d.stage==='Won').length}</b></div></div><div class="card" style="margin-top:10px"><div class="ey">My work</div><h3 style="margin:4px 0 8px">Next actions</h3><div class="compact-list">${mine.filter(d=>!['Won','Lost'].includes(d.stage)).slice(0,8).map(d=>`<div class="compact-row"><div><div class="title">${esc(d.deal_name)}</div><div class="meta">${esc(d.next_action||'No next action')}</div></div><span class="badge">${esc(String(d.next_action_date||'—').slice(0,10))}</span></div>`).join('')||'<div class="sub">No open deals.</div>'}</div></div>`}async function loadUsers(){if(!admin())return;try{const r=await apiFast('listUsers',{},1800);if(!r.ok)throw Error(r.error);$('usersWrap').innerHTML=`<div class="sub" style="margin:8px 0">Admin can issue, reset, activate or deactivate credentials directly here.</div><table><tr><th>User ID</th><th>PIN</th><th>Role</th><th>Name</th><th>Status</th><th>Actions</th></tr>${r.users.map(u=>`<tr><td><span class="user-cred">${esc(u.user_id)}</span></td><td><span class="user-cred">${esc(u.visible_pin||'')}</span></td><td>${esc(u.role)}</td><td>${esc(u.display_name)}</td><td><span class="badge">${String(u.active)==='false'?'Inactive':'Active'}</span></td><td><div class="rowactions"><button class="btn light sm" onclick='openUser(${JSON.stringify(JSON.stringify(u))})'>Edit</button><button class="btn light sm" onclick="resetPin('${esc(u.user_id)}')">Reset PIN</button><button class="btn ${String(u.active)==='false'?'o':'red'} sm" onclick="toggleUserActive('${esc(u.user_id)}',${String(u.active)==='false'})">${String(u.active)==='false'?'Activate':'Deactivate'}</button></div></td></tr>`).join('')}</table>`}catch(e){$('usersWrap').textContent=e.message}}setTimeout(()=>{try{if(me){renderProducts();loadDashboard()}}catch{}},300);